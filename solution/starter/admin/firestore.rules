rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV / TEST ONLY:
    // Allow anyone (including unauthenticated users) to READ anything.
    // Block all writes from clients.
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
